# Paso 4: Template para Loader

**Duracion estimada:** 20 minutos
**Prerequisitos:** Paso 3 completado

## Objetivo
Crear template que genera load_data.go con datos hardcodeados.

## Codigo a Implementar

Template simplificado (los datos reales se agregaran en pasos posteriores):

```go
const loaderTemplate = ` + "`" + `// Code generated by mock-generator. DO NOT EDIT.

package dataset

import (
	"time"
	"github.com/EduGoGroup/edugo-infrastructure/postgres/entities"
	"github.com/google/uuid"
)

func LoadAllData() {
{{- range .Tables }}
	load{{ .Name }}()
{{- end }}
}

{{- range .Tables }}

func load{{ .Name }}() {
	// TODO: Cargar datos de {{ .Name }}
}
{{- end }}
` + "`" + `
```

## Validacion

```bash
grep -q "loaderTemplate" pkg/generator/dataset_generator.go && echo "OK"
```

## Siguiente Paso
→ [paso-05-generar-database.md]
